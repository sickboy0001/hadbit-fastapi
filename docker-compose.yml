services:
  web:
    container_name: testloginrendar-app
    build: .
    ports:
      - "8000:10000"
    env_file:
      - .env
    volumes:
      - .:/app
    # 開発時はコード変更を検知して再起動するように --reload を追加
    command: uvicorn app.main:app --host 0.0.0.0 --port 10000 --reload